
from rai_digital_twin.prepare_data import *

def test_retrieval():
    # N = 2
    # price_df = retrieve_eth_price(N)
    # assert len(price_df) == N

    # hourly_df = retrieve_hourly_stats(price_df.index)
    # assert len(hourly_df) <= N

    # state_df = retrieve_system_states(price_df.index)
    # assert len(state_df) <= N

    # safe_df = retrieve_safe_history(price_df.index)
    # assert len(safe_df) <= N

    # df = download_data(N)
    # assert len(df) <= N

    assert True